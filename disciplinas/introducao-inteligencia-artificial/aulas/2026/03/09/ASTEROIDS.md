## results of a neural network

results = generation[].item.nn.layer[].neuron[].weight

## explainning

A generation has one Item.

An Item has one Neural Network.

A Neural Network has many Layers.

Each Layer has many Neurons.

Each Neuron has one Weight.

Get the Weights.

## 

Just `activate` function changes, implicitly the weights.
